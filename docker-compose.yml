services:
  app:
    build: .
    ports:
      - "3005:3005"
    depends_on:
      - mongo
    environment:
      - MONGO_URI=mongodb+srv://dhananjay991145_db_user:vRjd6mnBBZTHjVsF@cluster0.ul42xtt.mongodb.net/notesDB

  mongo:
    image: mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
